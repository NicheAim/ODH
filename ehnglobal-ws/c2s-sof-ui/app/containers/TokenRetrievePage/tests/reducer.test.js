
// import { fromJS } from 'immutable';
// import tokenRetrievePageReducer from '../reducer';

describe('tokenRetrievePageReducer', () => {
  it('returns the initial state', () => {
    // TODO: add unit tests
    // expect(tokenRetrievePageReducer(undefined, {})).toEqual(fromJS({}));
  });
});
