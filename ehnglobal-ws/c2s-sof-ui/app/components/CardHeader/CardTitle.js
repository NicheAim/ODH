/**
 *
 * CardTitle
 *
 */
import styled from 'styled-components';

const CardTitle = styled.div`
  font-size: 1.3rem;
  font-weight: bold;
  color: #366;
`;

CardTitle.propTypes = {};

export default CardTitle;
