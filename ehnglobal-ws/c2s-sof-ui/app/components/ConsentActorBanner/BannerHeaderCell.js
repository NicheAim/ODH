import styled from 'styled-components';
import { Cell } from 'styled-css-grid';

const BannerHeaderCell = styled(Cell)`
  background-color: rgba(240, 248, 250, 1);
  padding: 0 3px;
`;

BannerHeaderCell.propTypes = {};

export default BannerHeaderCell;
