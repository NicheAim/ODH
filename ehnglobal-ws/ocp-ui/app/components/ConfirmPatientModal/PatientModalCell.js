import { Cell } from 'styled-css-grid';

const PatientModalCell = Cell.extend`
  background-color: rgba(242, 242, 242, 1);
  min-height: 30px;
`;

PatientModalCell.propTypes = {};

export default PatientModalCell;
