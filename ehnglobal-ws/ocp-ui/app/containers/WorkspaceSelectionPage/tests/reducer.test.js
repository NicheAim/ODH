// import workspaceSelectionPageReducer from '../reducer';

describe('workspaceSelectionPageReducer', () => {
  it('returns the initial state', () => {
    // FIXME: add unit tests
    // expect(workspaceSelectionPageReducer(undefined, {})).toEqual(fromJS({}));
  });
});
