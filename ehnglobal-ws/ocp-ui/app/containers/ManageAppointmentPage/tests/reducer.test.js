// import manageAppointmentPageReducer from '../reducer';

describe('manageAppointmentPageReducer', () => {
  it('returns the initial state', () => {
    // FIXME: add unit tests
    // expect(manageAppointmentPageReducer(undefined, {})).toEqual(fromJS({}));
  });
});
