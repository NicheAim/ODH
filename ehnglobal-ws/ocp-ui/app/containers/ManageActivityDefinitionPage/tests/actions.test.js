
// import {
//   defaultAction,
// } from '../actions';
// import {
//   DEFAULT_ACTION,
// } from '../constants';

describe('ManageActivityDefinitionPage actions', () => {
  describe('Default Action', () => {
    it('has a type of DEFAULT_ACTION', () => {
      // FIXME: add unit tests
      // const expected = {
      //   type: DEFAULT_ACTION,
      // };
      // expect(defaultAction()).toEqual(expected);
    });
  });
});
