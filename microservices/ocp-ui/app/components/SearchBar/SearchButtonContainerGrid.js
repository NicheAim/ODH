import { Grid } from 'styled-css-grid';

const SearchButtonContainerGrid = Grid.extend`
  padding-left: 20px;
  padding-top: 5px;
  padding-bottom: 15px;
`;

SearchButtonContainerGrid.propTypes = {};

export default SearchButtonContainerGrid;
