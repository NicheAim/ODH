import styled from 'styled-components';

const ParticipantSearchContainer = styled.div`
  position: relative;
  padding: 0 10px;
  margin-bottom: 5px;
`;

ParticipantSearchContainer.propTypes = {};

export default ParticipantSearchContainer;
