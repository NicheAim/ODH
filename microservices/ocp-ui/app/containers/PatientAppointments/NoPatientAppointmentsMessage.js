import styled from 'styled-components';

const NoPatientAppointmentsMessage = styled.div`
  margin: 20px 10px;
  font-weight: bold;
`;

NoPatientAppointmentsMessage.propTypes = {};

export default NoPatientAppointmentsMessage;
