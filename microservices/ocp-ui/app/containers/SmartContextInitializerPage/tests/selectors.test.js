// import { fromJS } from 'immutable';
// import { selectSmartContextInitializerPageDomain } from '../selectors';

// TODO: add unit tests
xdescribe('selectSmartContextInitializerPageDomain', () => {
  it('Expect to have unit tests specified', () => {
    expect(true).toEqual(false);
  });
});
