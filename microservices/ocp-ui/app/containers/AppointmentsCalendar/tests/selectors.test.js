// import { fromJS } from 'immutable';
// import { selectAppointmentsCalendarDomain } from '../selectors';

describe('selectAppointmentsCalendarDomain', () => {
  it('Expect to have unit tests specified', () => {
   // expect(true).toEqual(false);
  });
});
